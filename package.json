{
  "name": "02-weather",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "build": "bun build --compile index.ts --outfile ./out/weather",
    "start": "bun run index.ts",
    "dev": "bun run index.ts --watch"
  },
  "dependencies": {
    "picocolors": "^1.1.1"
  }
}
